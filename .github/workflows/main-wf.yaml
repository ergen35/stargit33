name: "main workflow"

on:
  push:
    branches:
      - "main"
      - "master"
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: run dotnet
        run: dotnet --help
      - name: restore packages
        run: echo Hello, world!
